<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Login contra la tabla users de Supabase (HU-10) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- GPS: solo en primer plano y solo al registrar una devolución (sin ubicación en segundo plano).
         Android 12+ exige pedir FINE junto con COARSE; el usuario puede elegir la aproximada. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- Recordatorio de devolución (HU-09): Android 13+ lo pide al tener un préstamo entregado -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Evidencia fotográfica (HU-08): se pide al tocar "Adjuntar evidencia", no al abrir la app.
         Al declararlo, Android exige concederlo también para usar la app de cámara del sistema. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:name=".PrestamoLabApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrestamoLab">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PrestamoLab"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- La cámara del sistema escribe la foto en files/evidencias a través de este proveedor -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/rutas_fotos" />
        </provider>
    </application>

</manifest>